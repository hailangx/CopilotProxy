{
  "name": "copilot-proxy",
  "version": "1.0.0",
  "description": "Proxy server to translate Claude/Anthropic API requests to GitHub Copilot API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
